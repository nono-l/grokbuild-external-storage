# アプリを足す

両方にプロファイルを 1 枚ずつ足す。

```
vercel-hobby/apps/{id}/     Hobby 側の型・appId
php-api/apps/{id}.php       サーバの名前・CORS
```

雛形: `vercel-hobby/apps/_template` と `php-api/apps/_template.php`。  
`php-api/apps/active.php` はそのホスト管理画面のタイトルだけ。
